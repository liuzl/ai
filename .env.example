# .env.example - Configuration for the Go AI library examples
# Copy this file to .env and fill in your actual credentials.
# The .env file is ignored by git.

# --- General Configuration ---
# Set the provider to either "openai" or "gemini"
AI_PROVIDER=openai

# --- OpenAI Configuration ---
# (Required if AI_PROVIDER is "openai")
OPENAI_API_KEY="your-openai-api-key-here"
OPENAI_MODEL="gpt-4o-mini"
# OPENAI_BASE_URL="" # Optional: Uncomment to use a proxy or custom endpoint

# --- Google Gemini Configuration ---
# (Required if AI_PROVIDER is "gemini")
GEMINI_API_KEY="your-gemini-api-key-here"
GEMINI_MODEL="gemini-1.5-flash"
# GEMINI_BASE_URL="" # Optional: Uncomment to use a custom endpoint
