# AI Gateway Environment Configuration
# Copy this file to .env and fill in your credentials

# ============================================
# Backend Provider Credentials
# ============================================

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_BASE_URL=  # Optional: custom endpoint (e.g., Azure OpenAI)

# Anthropic Configuration
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
# ANTHROPIC_BASE_URL=  # Optional: custom endpoint

# Gemini Configuration
GEMINI_API_KEY=AIza-your-gemini-api-key-here
# GEMINI_BASE_URL=  # Optional: custom endpoint

# ============================================
# Server Configuration (Optional)
# ============================================

# Server listen address (default: :8080)
# PROXY_LISTEN_ADDR=:8080

# Path to YAML configuration file (default: config/proxy-config.yaml)
# PROXY_CONFIG_FILE=config/proxy-config.yaml

# Request timeout (default: 5m)
# PROXY_TIMEOUT=5m

# ============================================
# Notes
# ============================================
# - Only set the API keys for providers you want to use
# - The gateway will validate credentials on startup
# - BASE_URL is optional and only needed for custom endpoints
# - You can override these values with command-line flags
